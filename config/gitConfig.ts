export const GIT_CONFIG = {
  github: {
    username: 'dhiraj-gede', // Your GitHub org/user name. (This is the only required config)
  },
  base: '/gitprofile/', // Base URL for your GitHub profile
};
